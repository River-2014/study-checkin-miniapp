.paper-page {
  background: #FFF8E7;
  min-height: 100vh;
}

/* ===== 模式选择 ===== */
.mode-selector {
  display: flex;
  background: #FFF;
  border-radius: 16rpx;
  overflow: hidden;
  margin-bottom: 24rpx;
  box-shadow: 0 2rpx 12rpx rgba(0,0,0,0.06);
}
.mode-tab {
  flex: 1;
  text-align: center;
  padding: 24rpx 0;
  font-size: 30rpx;
  font-weight: 500;
  color: #636E72;
  transition: all 0.2s;
}
.mode-active {
  color: #FFF;
  background: linear-gradient(135deg, #FF9F43, #FF6B6B);
}

/* ===== 试卷筛选 ===== */
.paper-filter {
  display: flex;
  align-items: center;
  gap: 12rpx;
  flex-wrap: wrap;
}
.paper-filter .picker-link {
  font-size: 28rpx;
  color: #FF9F43;
  font-weight: 500;
}
.filter-search {
  flex: 1; min-width: 160rpx; height: 60rpx;
  border: 2rpx solid #E8E8E8; border-radius: 10rpx;
  padding: 0 12rpx; font-size: 26rpx;
  box-sizing: border-box;
}

/* ===== 试卷列表 ===== */
.paper-list-loading {
  text-align: center;
  padding: 60rpx 0;
  color: #B2BEC3;
  font-size: 28rpx;
}
.empty-hint {
  text-align: center;
  padding: 60rpx 0;
  display: flex;
  flex-direction: column;
  gap: 12rpx;
}
.empty-hint text:first-child {
  font-size: 30rpx; color: #B2BEC3;
}
.empty-sub {
  font-size: 24rpx !important;
  color: #C8C8C8 !important;
}

.paper-card {
  background: #FFF;
  border-radius: 16rpx;
  padding: 24rpx;
  margin-bottom: 16rpx;
  box-shadow: 0 2rpx 8rpx rgba(0,0,0,0.05);
  border-left: 6rpx solid #FF9F43;
}
.paper-card-head {
  margin-bottom: 10rpx;
}
.paper-title {
  font-size: 28rpx; font-weight: 600; color: #2D3436;
  line-height: 1.4;
}
.paper-card-meta {
  display: flex; flex-wrap: wrap; gap: 8rpx 16rpx;
  font-size: 24rpx; color: #636E72;
  margin-bottom: 8rpx;
}
.paper-card-tap {
  text-align: right;
  font-size: 24rpx; color: #FF9F43; font-weight: 500;
}

/* 分页 */
.pagination {
  display: flex; align-items: center;
  justify-content: center; gap: 16rpx;
  margin-top: 20rpx;
}
.page-info {
  font-size: 26rpx; color: #636E72;
}

/* ===== 设置面板 ===== */
.setting-panel { padding: 24rpx; }

.filter-group {
  margin-bottom: 24rpx;
}
.filter-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 16rpx 0;
  border-bottom: 1rpx solid #F0F0F0;
}
.filter-label {
  font-size: 28rpx;
  color: #2D3436;
  font-weight: 500;
}
.picker-value {
  font-size: 28rpx;
  color: #FF9F43;
}

/* 题型分布 */
.type-dist-grid {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
  margin-top: 12rpx;
}
.dist-item {
  padding: 12rpx 24rpx;
  border-radius: 30rpx;
  background: #F0F0F0;
  text-align: center;
  border: 2rpx solid transparent;
}
.dist-active {
  background: #FFF3E0;
  border-color: #FF9F43;
}
.dist-type {
  display: block;
  font-size: 24rpx;
  color: #636E72;
}
.dist-active .dist-type {
  color: #FF9F43;
}
.dist-count {
  font-size: 28rpx;
  font-weight: bold;
  color: #2D3436;
}

/* ===== 答题顶部栏 ===== */
.exam-topbar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 16rpx 24rpx;
  background: #fff;
  border-bottom: 1rpx solid #F0F0F0;
  font-size: 26rpx;
}
.exam-title {
  color: #2D3436;
  font-weight: 500;
}
.countdown {
  color: #FF9F43;
  font-weight: bold;
}
.countdown-warn {
  color: #FF6B6B;
  animation: blink 1s infinite;
}
@keyframes blink {
  50% { opacity: 0.5; }
}

/* 进度 */
.question-progress {
  padding: 12rpx 24rpx;
  background: #fff;
  border-bottom: 1rpx solid #F0F0F0;
}
.progress-info {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 8rpx;
  font-size: 24rpx;
  color: #636E72;
}
.submit-early {
  color: #FF9F43;
  font-weight: 500;
}
.progress-bar-bg {
  height: 4rpx;
  background: #EEE;
  border-radius: 2rpx;
}
.progress-bar-fill {
  height: 100%;
  background: #FF9F43;
  border-radius: 2rpx;
  transition: width 0.3s;
}

/* Swiper */
.question-swiper {
  width: 100%;
  height: calc(100vh - 340rpx) !important;
}
.question-scroll {
  height: 100%;
  padding: 24rpx;
}
.question-card {
  background: #fff;
  border-radius: 20rpx;
  padding: 28rpx;
  margin-bottom: 16rpx;
}
.q-header {
  display: flex;
  align-items: center;
  gap: 12rpx;
  margin-bottom: 16rpx;
}
.q-num {
  font-size: 32rpx;
  font-weight: bold;
  color: #2D3436;
}
.q-type-tag {
  font-size: 22rpx;
  background: #FFF3E0;
  color: #FF9F43;
  padding: 4rpx 12rpx;
  border-radius: 8rpx;
}
.q-pts {
  font-size: 22rpx;
  color: #B2BEC3;
}
.q-stem {
  font-size: 30rpx;
  color: #2D3436;
  line-height: 1.8;
  margin-bottom: 24rpx;
}

/* 选择题 */
.options {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}
.option {
  display: flex;
  align-items: center;
  padding: 20rpx 24rpx;
  border-radius: 16rpx;
  border: 2rpx solid #E8E8E8;
  font-size: 28rpx;
  transition: all 0.2s;
}
.option-selected {
  border-color: #FF9F43;
  background: #FFF3E0;
}
.option-correct {
  border-color: #2ED573;
  background: #E8F5E9;
}
.option-wrong {
  border-color: #FF6B6B;
  background: #FFE0E0;
}
.opt-label {
  font-weight: bold;
  margin-right: 12rpx;
  color: #636E72;
  width: 36rpx;
}

/* 判断题 */
.judge-btns {
  display: flex;
  gap: 24rpx;
}
.judge-btn {
  flex: 1;
  text-align: center;
  padding: 24rpx;
  border-radius: 16rpx;
  border: 2rpx solid #E8E8E8;
  font-size: 30rpx;
  font-weight: bold;
  color: #636E72;
  transition: all 0.2s;
}
.judge-active {
  border-color: #FF9F43;
  background: #FFF3E0;
  color: #FF9F43;
}
.judge-active.judge-wrong {
  border-color: #FF6B6B;
  background: #FFE0E0;
  color: #FF6B6B;
}

/* 填空 */
.blank-input {
  width: 100%;
  height: 80rpx;
  border: 2rpx solid #E8E8E8;
  border-radius: 12rpx;
  padding: 0 20rpx;
  font-size: 28rpx;
  box-sizing: border-box;
}

/* 简答 */
.essay-textarea {
  width: 100%;
  height: 200rpx;
  border: 2rpx solid #E8E8E8;
  border-radius: 12rpx;
  padding: 16rpx;
  font-size: 28rpx;
  box-sizing: border-box;
}

/* 解析 */
.analysis {
  margin-top: 24rpx;
  padding: 20rpx;
  background: #FAFAFA;
  border-radius: 12rpx;
}
.correct-badge {
  font-size: 28rpx;
  color: #2ED573;
  font-weight: bold;
  margin-bottom: 8rpx;
}
.wrong-badge {
  font-size: 28rpx;
  color: #FF6B6B;
  font-weight: bold;
  margin-bottom: 8rpx;
}
.correct-answer {
  font-size: 28rpx;
  color: #2ED573;
  font-weight: bold;
  margin-bottom: 8rpx;
}
.analysis-text {
  font-size: 26rpx;
  color: #636E72;
  line-height: 1.6;
}

/* 提交栏 */
.submit-bar {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  padding: 20rpx 24rpx;
  padding-bottom: calc(20rpx + env(safe-area-inset-bottom));
  background: #fff;
  box-shadow: 0 -2rpx 10rpx rgba(0,0,0,0.05);
}

/* ===== 成绩报告 ===== */
.result-panel {
  padding: 24rpx;
  padding-bottom: calc(40rpx + env(safe-area-inset-bottom));
}
.score-card {
  text-align: center;
  padding: 40rpx;
  background: linear-gradient(135deg, #FF9F43, #FDCB6E);
  border-radius: 20rpx;
  margin-bottom: 24rpx;
  color: #fff;
}
.score-big {
  font-size: 80rpx;
  font-weight: bold;
  display: block;
}
.score-total {
  font-size: 32rpx;
  opacity: 0.8;
  display: block;
  margin-bottom: 8rpx;
}
.score-rate {
  font-size: 26rpx;
  opacity: 0.7;
  display: block;
}
.section-title {
  font-size: 30rpx;
  font-weight: 700;
  color: #2D3436;
  padding: 10rpx 0 16rpx;
}
.result-actions {
  display: flex;
  gap: 16rpx;
  margin-top: 24rpx;
}
.result-actions .btn {
  flex: 1;
  margin: 0;
}
